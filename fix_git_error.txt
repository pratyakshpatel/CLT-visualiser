# fix for "src refspec main does not match any" error

# step 1: configure git with your github credentials
git config --global user.name "pratyakshpatel"
git config --global user.email "pratyakshpatel1809@gmail.com"

# step 2: verify configuration
git config --global user.name
git config --global user.email

# step 3: check your current status
git status

# step 4: check what branch you're on
git branch

# step 5: make sure all files are added
git add .

# step 6: make your initial commit (this creates the branch)
git commit -m "initial commit: central limit theorem visualizer"

# step 7: now check your branch again
git branch

# step 8: rename your branch to main if it's not already
git branch -M main

# step 9: verify remote is set correctly
git remote -v

# step 10: now try pushing (you'll need personal access token as password)
git push -u origin main